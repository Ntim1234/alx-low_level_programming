#include "main.h"

/**
 * _abs - prints the absolute value of an integer
 * @t: the integer to be printed
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
